import Typography from "@mui/material/Typography";

const Title = () => {
    return (
        <Typography sx={{ mb: "16px" }} variant="h5" gutterBottom component="div">
            Mobx Todo List
        </Typography>
    );
};

export default Title;
